we use python3 to write this homework, and we add #!usr/bin/env/python3 in the first line of our codes
and the command we need to type is as the followings:
chmod +x mitm_attack
chmod +x pharm_attack
sudo ./mitm_attack
sudo ./pharm_attack